<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title>User Dashboard</title>
</head>
<body>
    <%- include('./partials/nav') %>
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-12 text-center">
                <p class="text-info fw-bolder fs-1">Welcome, <span><u><%- locals.user.name %></u></span> !</p>
                <p class="text-secondary fs-5">Here is a quick summary of your tasks.</p>
            </div>
        </div>
    </div>

    <div class="container mt-3">
        <div class="row text-center">
            <div class="col-md-4">
                <div class="card shadow-sm p-3">
                    <h4><%-totalTasks %></h4>
                    <p class="text-muted fs-4">Total Tasks</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card shadow-sm p-3">
                    <h4><%-pendingTasks %></h4>
                    <p class="text-muted fs-4">Pending Tasks</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card shadow-sm p-3">
                    <h4><%-completedTasks %></h4>
                    <p class="text-muted fs-4">Completed Tasks</p>
                </div>
            </div>
        </div>
    </div>

    <div class="container mt-4 text-center">
        <a href="/api/allTasks" class="btn btn-primary">View All Tasks</a>
        <a href="/createTask" class="btn btn-success">Add New Task</a>
    </div>
    <%- include('./partials/script') %>
</body>
</html>